# Structure

Included runnable stages are `EDA/`, `EXPERIMENT/`, `ABLATION/`, and `EVALUATION/`. Each included stage contains its available `scripts/`, `notebooks/`, `results/`, and `DOCUMANTATION/` assets. Source code is under `src/`, and final paper/version evidence is under `PAPER_ASSETS/`.
